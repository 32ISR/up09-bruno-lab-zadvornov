info:
  name: get_my_bids
  type: http
  seq: 18

http:
  method: GET
  url: "{{base_url}}/api/bids/my"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
