info:
  name: create_item
  type: http
  seq: 12

http:
  method: POST
  url: "{{base_url}}/api/items"
  body:
    type: json
    data: |-
      {
        "title": "iPhone 9",
        "description": "Новый iPhone 9",
        "price": 8000000000000,
        "imageUrl": "https://example.com/iphone.jpg"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
