info:
  name: get_bids
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/items/1773225661793:id/bids"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
