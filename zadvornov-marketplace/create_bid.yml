info:
  name: create_bid
  type: http
  seq: 16

http:
  method: POST
  url: "{{base_url}}/api/items/:id/bids"
  params:
    - name: id
      value: "1773221650075"
      type: path
  body:
    type: json
    data: |-
      {
        "amount": 85000000000000000000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
